# Central allowlist for pip-audit vulnerability IDs used by CI and local release gates.
# Format: VULN-ID YYYY-MM-DD # rationale
# Keep entries narrow, documented, and temporary. Remove each entry once the
# underlying dependency or runner image can be updated without blocking unrelated PRs.

# anyio 4.13.0 transitive dependency CVEs; permanent fix is to pin anyio>=4.14.2 in
# pyproject.toml dependencies (tracked separately — not in scope for vision-layer PRs).
CVE-2026-64847 2026-12-31 # anyio 4.13.0 transitive dep; fix by pinning anyio>=4.14.2.
CVE-2026-63374 2026-12-31 # anyio 4.13.0 transitive dep; fix by pinning anyio>=4.14.2.
