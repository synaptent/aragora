name: Publish Python SDK to PyPI

# Publishes `aragora-sdk` from sdk/python/ to PyPI.
# Triggers on version tags or manual dispatch.

on:
  push:
    tags:
      - 'sdk-python-v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 2.7.5)'
        required: true
        type: string
      dry_run:
        description: 'Dry run only (skip actual publish)'
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  id-token: write  # Required for PyPI trusted publishing

concurrency:
  group: publish-python-sdk-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test:
    runs-on: aragora
    timeout-minutes: 15
    name: Test Python SDK (Python ${{ matrix.python-version }})
    strategy:
      fail-fast: true
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    defaults:
      run:
        working-directory: sdk/python

    steps:
      - uses: actions/checkout@v4

      - name: Verify checkout integrity
        shell: bash
        run: |
          if [[ ! -f pyproject.toml ]]; then
            echo "::warning::pyproject.toml missing after checkout; attempting recovery"
            git sparse-checkout disable || true
            git fetch --no-tags origin "${GITHUB_SHA:-HEAD}"
            git reset --hard FETCH_HEAD
            git clean -ffd || true
          fi
          if [[ ! -f pyproject.toml ]]; then
            echo "::warning::Standard recovery failed; attempting archive restore"
            find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
            git archive "${GITHUB_SHA:-HEAD}" | tar -x || git archive HEAD | tar -x
          fi
          if [[ ! -f pyproject.toml ]]; then
            echo "::error::Repository checkout is incomplete (pyproject.toml missing)"
            echo "PWD=$(pwd)"
            ls -la
            exit 1
          fi

      - name: Setup Python ${{ matrix.python-version }}
        uses: ./.github/actions/setup-python-safe
        with:
          python-version: "${{"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Lint
        run: python -m ruff check aragora_sdk/ tests/

      - name: Type check
        if: matrix.python-version == '3.11'
        run: python -m mypy aragora_sdk/ --strict || true

      - name: Run tests
        run: python -m pytest tests/ -x -q --tb=short

  build-and-publish:
    needs: test
    runs-on: aragora
    timeout-minutes: 10
    environment: ${{ (github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true') && '' || 'pypi' }}

    defaults:
      run:
        working-directory: sdk/python

    steps:
      - uses: actions/checkout@v4

      - name: Verify checkout integrity
        shell: bash
        run: |
          if [[ ! -f pyproject.toml ]]; then
            echo "::warning::pyproject.toml missing after checkout; attempting recovery"
            git sparse-checkout disable || true
            git fetch --no-tags origin "${GITHUB_SHA:-HEAD}"
            git reset --hard FETCH_HEAD
            git clean -ffd || true
          fi
          if [[ ! -f pyproject.toml ]]; then
            echo "::warning::Standard recovery failed; attempting archive restore"
            find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
            git archive "${GITHUB_SHA:-HEAD}" | tar -x || git archive HEAD | tar -x
          fi
          if [[ ! -f pyproject.toml ]]; then
            echo "::error::Repository checkout is incomplete (pyproject.toml missing)"
            echo "PWD=$(pwd)"
            ls -la
            exit 1
          fi

      - name: Setup Python
        uses: ./.github/actions/setup-python-safe
        with:
          python-version: "3.11"

      - name: Install build tools
        run: python -m pip install build hatchling twine

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            # Extract version from tag: sdk-python-v1.2.3 -> 1.2.3
            VERSION="${GITHUB_REF_NAME#sdk-python-v}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Update version in pyproject.toml
        run: |
          sed -i 's/version = "[^"]*"/version = "${{ steps.version.outputs.version }}"/' pyproject.toml
          echo "Updated pyproject.toml:"
          grep 'version =' pyproject.toml

      - name: Build package
        run: python -m build

      - name: Dry run -- check package
        run: |
          python -m twine check dist/*
          echo ""
          echo "=== Package contents ==="
          ls -la dist/
          echo ""
          echo "=== Package metadata ==="
          python -m pip install dist/*.whl --dry-run 2>&1 | head -20 || true

      - name: Check version not already published
        if: ${{ !(github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true') }}
        run: |
          python -m pip install aragora-sdk==${{ steps.version.outputs.version }} --dry-run 2>/dev/null && {
            echo "::error::Version ${{ steps.version.outputs.version }} already exists on PyPI"
            exit 1
          } || echo "Version ${{ steps.version.outputs.version }} not yet published -- OK"

      - name: Publish to PyPI (trusted publishing)
        if: ${{ !(github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true') }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: sdk/python/dist/

      - name: Publish to PyPI (API token fallback)
        if: ${{ failure() && !(github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true') }}
        run: python -m twine upload dist/*
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}

      - name: Dry run summary
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true' }}
        run: |
          echo "::notice::DRY RUN complete. Package built and validated but NOT published."
          echo "Package: aragora-sdk==${{ steps.version.outputs.version }}"
          echo "Files:"
          ls -la dist/

      - name: Create GitHub Release
        if: ${{ github.event_name == 'push' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Python SDK v${{ steps.version.outputs.version }}
          body: |
            ## Python SDK v${{ steps.version.outputs.version }}

            Install with:
            ```bash
            python -m pip install aragora-sdk==${{ steps.version.outputs.version }}
            ```

            See [documentation](https://docs.aragora.ai/sdk/python) for usage.
          draft: false
          prerelease: ${{ contains(steps.version.outputs.version, 'beta') || contains(steps.version.outputs.version, 'alpha') || contains(steps.version.outputs.version, 'rc') }}
          files: |
            sdk/python/dist/*
