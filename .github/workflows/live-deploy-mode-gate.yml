name: Live Deploy Mode Gate

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - 'aragora/live/**'
      - '.github/workflows/live-deploy-mode-gate.yml'
      - '.github/workflows/deploy-frontend.yml'
      - '.github/workflows/deploy-secure.yml'
  schedule:
    # Nightly at 1:55 UTC
    - cron: '55 1 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: live-deploy-mode-gate-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  scope:
    name: Live Deploy Scope
    runs-on: ubuntu-latest
    outputs:
      deploy_relevant: ${{ steps.defaults.outputs.deploy_relevant || steps.filter.outputs.deploy_relevant }}
    steps:
      - name: Default scope (non-PR)
        if: github.event_name != 'pull_request'
        id: defaults
        run: echo "deploy_relevant=true" >> "$GITHUB_OUTPUT"

      - name: Checkout
        if: github.event_name == 'pull_request'
        uses: actions/checkout@v4

      - name: Detect deploy-impacting frontend changes
        if: github.event_name == 'pull_request'
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            deploy_relevant:
              - 'aragora/live/package*.json'
              - 'aragora/live/next.config.*'
              - 'aragora/live/tsconfig*.json'
              - 'aragora/live/playwright*.config.*'
              - 'aragora/live/e2e/**'
              - 'aragora/live/functions/**'
              - 'aragora/live/public/**'
              - 'aragora/live/scripts/**'
              - 'aragora/live/src/api/**'
              - 'aragora/live/src/app/**'
              - 'aragora/live/src/components/**'
              - 'aragora/live/src/context/**'
              - 'aragora/live/src/hooks/**'
              - 'aragora/live/src/lib/**'
              - 'aragora/live/src/store/**'
              - 'aragora/live/src/styles/**'
              - 'aragora/live/src/utils/**'
              - '.github/workflows/live-deploy-mode-gate.yml'
              - '.github/workflows/deploy-frontend.yml'
              - '.github/workflows/deploy-secure.yml'

      - name: Skip reason
        if: github.event_name == 'pull_request' && steps.filter.outputs.deploy_relevant != 'true'
        run: |
          echo "No deploy-impacting frontend files changed; skipping self-hosted live deploy mode gate."

  gate:
    needs: scope
    if: (github.event_name != 'pull_request' || !github.event.pull_request.draft) && (github.event_name != 'pull_request' || needs.scope.outputs.deploy_relevant == 'true')
    name: Validate Live Deploy Mode
    runs-on: [self-hosted, Linux, X64, aragora, hetzner]
    timeout-minutes: 30
    env:
      NODE_VERSION: '24.18.0'
      LIVE_DEPLOY_MODE: ${{ vars.LIVE_DEPLOY_MODE }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify checkout integrity
        shell: bash
        run: |
          if [[ ! -f pyproject.toml ]]; then
            echo "::warning::pyproject.toml missing after checkout; attempting recovery"
            git sparse-checkout disable || true
            git fetch --no-tags origin "${GITHUB_SHA:-HEAD}"
            git reset --hard FETCH_HEAD
            git clean -ffd || true
          fi
          if [[ ! -f pyproject.toml ]]; then
            echo "::warning::Standard recovery failed; attempting archive restore"
            find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
            git archive "${GITHUB_SHA:-HEAD}" | tar -x || git archive HEAD | tar -x
          fi
          if [[ ! -f pyproject.toml ]]; then
            echo "::error::Repository checkout is incomplete (pyproject.toml missing)"
            echo "PWD=$(pwd)"
            ls -la
            exit 1
          fi

      - name: Resolve deploy mode
        id: mode
        run: |
          MODE="${LIVE_DEPLOY_MODE:-runtime}"
          if [ "$MODE" != "runtime" ] && [ "$MODE" != "export" ]; then
            echo "::error::LIVE_DEPLOY_MODE must be 'runtime' or 'export' (got '$MODE')"
            exit 1
          fi
          echo "mode=$MODE" >> "$GITHUB_OUTPUT"
          echo "Resolved LIVE_DEPLOY_MODE=$MODE"

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: aragora/live/package-lock.json

      - name: Install dependencies
        working-directory: aragora/live
        run: npm ci

      - name: Runtime mode checks
        if: steps.mode.outputs.mode == 'runtime'
        working-directory: aragora/live
        run: |
          npx tsc --noEmit
          npm run build
          if command -v apt-get >/dev/null 2>&1; then
            npx playwright install --with-deps chromium
          else
            echo "::notice::apt-get unavailable on runner; installing Chromium without OS package bootstrap"
            npx playwright install chromium
          fi
          npx playwright test \
            e2e/debate-viewing.spec.ts \
            e2e/debate-intervention-live.spec.ts \
            e2e/websocket.spec.ts \
            --project=chromium \
            --workers=1

      - name: Export mode checks
        if: steps.mode.outputs.mode == 'export'
        working-directory: aragora/live
        run: |
          if rg -n "export const dynamicParams = true" src/app; then
            echo "::error::dynamicParams=true is incompatible with output: export"
            exit 1
          fi
          npm run build:export
