# Copy to secrets.env, fill every value, then chmod 600 secrets.env.
# Never commit secrets.env or print its contents.

# ARAGORA_ENCRYPTION_KEY: openssl rand -hex 32
ARAGORA_ENCRYPTION_KEY=
# ARAGORA_JWT_SECRET: openssl rand -hex 32
ARAGORA_JWT_SECRET=
# POSTGRES_PASSWORD: openssl rand -hex 32 (URL-safe)
POSTGRES_PASSWORD=
# ARAGORA_API_TOKEN: openssl rand -hex 32
ARAGORA_API_TOKEN=
# Use postgresql://aragora:<POSTGRES_PASSWORD>@postgres:5432/aragora
DATABASE_URL=
