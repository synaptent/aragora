# Advisory only: there is no root package.json, and npm reads project config
# solely from the nearest package.json prefix (no parent merge). Each JS app
# carries the same key in its own .npmrc; this file covers future root-level
# packages and documents the policy in one place.
min-release-age=3
