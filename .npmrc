# Applies to npx/npm invocations run from the repo root, including Make's
# jscpd pin, even without a root package.json. npm does not merge parent
# project configs, so each nested JS app carries the same key in its own .npmrc.
min-release-age=3
