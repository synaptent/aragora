# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Git worktrees for parallel agent execution
.worktrees/

# Demo launcher PID/logs
.demo/

# Multi-agent coordination artifacts
.aragora_sessions.yaml
.aragora_sessions.json
.aragora_events/
.aragora_ideas/
.aragora_coordination/

# Worktree session lock files (created by SessionStart hook, removed on Stop)
.claude-session-active

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
coverage*.json
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
.mypy_cache/

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Node
node_modules/
typings/
docs-site/.docusaurus/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Project specific
*.db
*.db-shm
*.db-wal
*.db-journal
elo_snapshot.json
**/elo_snapshot.json
agora_memory.db
debates.db
*.sqlite
*.sqlite3

# Logs
*.log
logs/

# Local config
.env.local
config.local.yaml
.DS_Store

# Local drafts / scratch
drafts/

# Local exports / generated artifacts
exports/
artifacts/
# …but committed proof/compliance artifacts under docs/ are real documentation,
# not build output. Without this negation every gitignore-respecting tool
# (grep harnesses, doc-link validators, reviewers) believes they don't exist
# even though they are tracked (#8824 evidence round 1).
!docs/artifacts/
docs/artifacts/*
!docs/artifacts/2026-07-decision-integrity-dogfooding.md
!docs/artifacts/2026-07-reviewer-failure-taxonomy.md
!docs/artifacts/2026-07-10-stranger-sim-run-1.md
smoke_output/
replays/*.mp3

# Claude-mem context files
aragora/**/CLAUDE.md
scripts/CLAUDE.md

# Generated Claude context files (avoid committing new ones)
**/CLAUDE.md
!/CLAUDE.md

# Environment files (security - never commit secrets)
**/.env
**/.envrc
**/.env.local
**/.env.production
**/.env.*
**/.env.*.local
!**/.env.example
!**/.env.local.example
!**/.env.template
!**/.env.*.example

# Live dashboard
aragora/live/node_modules/
aragora/live/.next/
aragora/live/out/
aragora/live/docs/
tsconfig.tsbuildinfo
.env.production

# Playwright E2E tests
aragora/live/playwright-report/
aragora/live/playwright-report-production/
aragora/live/test-results/
aragora/live/playwright/.cache/
.playwright-cli/
.playwright-mcp/

# Test results (Playwright, pytest, etc.)
test-results/

# Test caches
.test_cache/
.benchmarks/

# Nomic loop state (runtime artifacts, not needed in git)
# The entire .nomic/ directory is runtime state -- databases, logs, checkpoints, etc.
.nomic/
.work_queue/

# Web cache
.web_cache/

# Local backups
backups/
.env_backups/
:memory:

# Convoy runtime state
.convoys/

# Screenshots (dev/testing artifacts)
FireShot*.png
Screenshot*.png
*.screenshot.png
output/
.mypy_cache/
typings/

# Investor documents (sensitive business documents)
docs/investor/
:memory:-shm
:memory:-wal

# Comparison projects (external references)
.comparison/
# Local config
.python-version
.claude/
.checkpoints/

# Bead tracking data (has its own git repo)
.beads/
.aragora_beads/
**/.aragora_beads/

# Build tool caches
.ruff_cache/
.grok/

# Approval testing artifacts
.approvals/

# Local data directories (runtime artifacts)
data/
consolidated/
.data/
.aragora/
/knowledge/
compliance-bundle/
docs/openapi_generated.json
.testfixer/
.tmp/
aragora-demo-receipt.json

# Personal/non-project files
shoggoth/
fire-alarm-essay-v3.md
unified-outline.md
MagicMock/

# Forensic scan outputs that contain real exposed secrets (would re-expose if committed)
benchmarks/bench_readiness/incident_scans/raw/
benchmarks/bench_readiness/incident_scans/RingRift_gitleaks.json
benchmarks/bench_readiness/incident_scans/aragora_gitleaks.json

# Repo-root hygiene — prevents re-introduction of cleaned clutter (cold audit 2026-04-18)
# Sales / strategy collateral kept out of the code repo
Enterprise_Multi_Agent_Control_Plane_Report.docx
/Aragora Idea-to-Execution Strategy.docx
# Unrelated images at repo root
/aragora icon.png
godmod3-homepage.png
# Test/runtime artifacts written to repo root
coverage-handlers.json
# Stale generated OpenAPI artifact (canonical copy lives at docs/api/openapi.json)
/openapi.json
# Inbox triage failure dump (contains a personal email; see docs/governance/gmail-failures-history-decision.md)
gmail_failures.json
# Landing-page marketing screenshots and auth captures
/landing-*.png
/openrouter-auth-page.png

# Machine-local gt store config (workspace path is per-clone)
.gt/config.json

# Readiness gate artifacts (make readiness-*): coverage, junit, jscpd reports,
# VS Code test-electron downloads and the mission venv. Non-anchored so the
# per-app copies (aragora/live, docs-site, ide/vscode-aragora, ...) are covered.
.vscode-test/
coverage/
junit.xml
.jscpd-report/
.venv-mission/
aragora/live/coverage/
aragora/live/junit.xml
docs-site/.jscpd-report/
ide/vscode-aragora/junit.xml
ide/vscode-aragora/.vscode-test/
aragora-operator/cover.out
