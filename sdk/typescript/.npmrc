# npm configuration for @aragora/sdk
# The @aragora scope is configured for npm public registry

# Registry configuration
@aragora:registry=https://registry.npmjs.org/

# Access level for scoped packages
access=public

# Only install package versions published at least 3 days ago
min-release-age=3
