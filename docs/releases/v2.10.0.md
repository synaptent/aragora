# Aragora v2.10.0

**Target release:** 2026-09-04, on the operator's `v2.10.0` tag
**Previous stable:** v2.9.0 (2026-04-25, sha `1e3ce85ae6`)
**Release-candidate predecessor:** none (cut directly from `main`; see "Reviewed design tradeoffs" in the release PR)

## Headline

The dissent-preserving Decision Receipt is now load-bearing end to end. A PR review run through `aragora review` produces an Open Decision Receipt (ODR), the receipt can be signed with a detached Ed25519 signature and served with its public key, and a stranger can verify it offline with the standalone `aragora-verify` package or with the in-package `odr_verify` engine. The GitHub Action emits the same receipt as a PR artifact.

Alongside the receipt path, merge-gate disagreement is now published as data: Disagreement Atlas v1 (`docs/atlas/`, release `atlas-v1`) records every counted reviewer verdict, adjudication and vindication across the merge quorum, and the receipt-first scoreboard (`scripts/receipt_first_scoreboard.py`) measures the ten exit metrics that the Receipt-First mission is held to.

This release carries 1,862 commits since v2.9.0. It is cut directly from `main` without a release-candidate tag because every one of those commits already landed behind the required check set (lint, typecheck, sdk-parity, Generate & Validate, TypeScript SDK Type Check, aragora-merge-quorum).

## What's new since v2.9.0

### Open Decision Receipt path
- ODR v1.0 contract and quorum→receipt bridge (#8667); `odr_verify` in-package verification engine (#8389)
- Ed25519 detached signing for ODR exports (#8542, #9080); public key at `/.well-known` and `/api/v2` (#8809)
- `aragora review` emits ODR receipts (#9343); planning evidence bound in receipt schema 1.3 (#9758)
- Receipts never mint PASS/APPROVED from zero evidence (#9306); reasoning no longer truncated (#9517)
- `aragora-verify` standalone offline verifier (#8388), PyPI publish workflow (#8693), post-publish install check (#8945), dependency policy enforced in CI (#9785)
- GitHub Action emits a verifiable Decision Receipt as a PR artifact (#8669)

### Disagreement Atlas and scoreboard
- Disagreement Atlas v1: merge-gate disagreement dataset with adjudication, summary tables and a hashed manifest (#9951); published as the `atlas-v1` release
- Receipt-first scoreboard: ten exit metrics, six guardrails, offline cache, epic posting (#9968)
- Crux cards name the load-bearing dissent in receipts (#9414, #9506); epistemic tags and dissent-protected situation envelopes (#9944)

### Merge quorum and settlement
- Tier quorum: Tier 0-2 settles on one western-frontier signal (#8507); default reviewer pair claude + openai (#8745)
- Severity-gated dissent (#8574), advisory-dissent settlement (#8729), Review Adjudicator (#8749), relief valve (#9203)
- `settle_pr` (#8511) and `settle_tier4_pr --skew-auto-resolve` (#8750); Tier-4 merge-train (#8414); unattended Tier 0-2 auto-merge (#8503, #8767)
- VibeProxy Claude quorum transport (#9483), conditionally countable evidence with grok verdict retry (#9770), grok 4.5 reviewer with Tier 3-4 counting narrowed to frontier Western families (#9205)

### Model transports and catalog
- VibeProxy-first Fable transport (#9408), exact OpenAI routing via VibeProxy (#9477)
- Canonical model catalog (#9355) feeding router pricing (#9364, #9374) and the decision-stakes router (#9322)
- Subscription-CLI fleet with cheap-tier routing and a fail-closed budget cap (#8481); OpenRouter Fusion as a flagged, budget-capped agent (#8444)
- Frontier defaults: GPT-5.6 Sol + Claude Fable 5 (#9075), Claude Opus 5 (#9588), Kimi K3 (#9778), Qwen 3.8 Max (#9783)

### Governance and structure
- Contract-drift authority roots, bootstrap and corrective authority (#9406, #9659, #9645); first paydown batches (#9969, #9976)
- Boundary 2 module-edge enforcement (#9787); truthful route-core operation plane (#9717, #9728); live workflow state verifier (#9745)
- Import-graph metrics with a shrink-only mutual-cycle ratchet (#8331, #9970); five cycle repairs bring `mutual_import_cycles` from 144 to 138 (#9971-#9975)
- Native mission engine: contracts, executor, orchestrator, CLI, control loop and intake bridge (#8465-#8486, #8655, #8766)
- Outcome-backed evaluation program with frozen benchmark conditions and custody ledgers (#9890-#9936); Ground-Truth Integrity benchmark (#7877)

### Packaging and security
- Root distribution installable as `aragora` (#8517); lazy connector imports so the base install runs `aragora ask` (#8522)
- aiohttp floor `>=3.14.3` and refreshed `uv.lock` CVE pins (#9714); Dependabot sweeps across the npm projects
- EU AI Act Art. 14 human-oversight attestation and oversight-pack generator (#9417)

Full grouped list: `CHANGELOG.md` `## [2.10.0]`.

### Housekeeping in the release PR
- The docs page count sits at its guardrail ceiling, so this new page is paired with the deletion of `docs/plans/2026-04-30-round-briefing.md`, a round briefing whose PRs (#6806-#6818) merged in April 2026 and which nothing links to.

## Upgrade notes

### From v2.9.x

No mandatory migration steps. The Python public API is additive. After the tag is cut:

```bash
pip install --upgrade aragora
python -c "import aragora; print(aragora.__version__)"   # 2.10.0
```

To verify receipts offline, install the standalone verifier alongside it:

```bash
pip install aragora-verify
aragora-verify --help
```

### Receipt producers and consumers

- `aragora review` and the GitHub Action now emit ODR documents; signing is opt-in and requires a configured Ed25519 key (`ARAGORA_ODR_SIGNING_KEY_SECRET` or the secrets-manager path). Unsigned receipts remain valid ODR documents.
- Receipt schema 1.3 adds optional planning-evidence bindings; consumers that ignore unknown members are unaffected.
- `DecisionReceipt` verdicts are never minted from zero evidence (#9306). Pipelines that relied on an empty-evidence PASS will now see a non-passing verdict.

### Model defaults

Runtime defaults moved to the current frontier models (Claude Fable 5 / Opus 5, GPT-5.6 Sol, Kimi K3, Qwen 3.8 Max). Pin `model=` explicitly where a fixed model matters; the canonical catalog (`aragora/models/`) is the single source for names and pricing.

### npm consumers

`aragora/live`, `sdk/typescript`, `ide/vscode-aragora`, `examples/sveltekit` and `docs-site` carry refreshed dependency pins; re-run `npm install` after pulling.

## Known issues / deferrals

- **`release.yml` `validate` job:** the tag-push workflow has failed its `validate` job on every tag push to date; PyPI 2.9.0 was published by `publish-aragora.yml` (`workflow_dispatch`). The same path is used for 2.10.0; `release.yml` is recorded, not required.
- **Whole-tree mypy:** `mypy aragora/` reports 1,744 errors on `main` and `make ci-required` is red; CI masks it and the mission gate only forbids growth.
- **Hosted API:** `api.aragora.ai` does not answer from outside; the canary answers 200. Cutover is an operator act tracked in #9391.
- **Postgres migrations:** `python -m aragora.migrations upgrade` against Postgres raises `psycopg2.errors.SyntaxError` on the SQLite-only `AUTOINCREMENT` DDL in `_init_rollback_history_table` (`aragora/migrations/runner.py:262`); the surrounding `except` names only `sqlite3.Error`/`OSError`/`RuntimeError`/`ValueError`, so the driver error escapes the "non-fatal" guard. The backend Docker image also cannot import its installed dependencies at runtime (#9955). Both are scheduled for the hosted-receipts milestone.
- **Import cycles:** 138 remain (ceiling 140); the `review_queue↔review_queue_{render,unstable}` pairs are Tier 4 and untouched.

## Verification

- `RELEASE_VERSION=2.10.0 python3 scripts/pre_release_check.py --gate version-tag -v` → `[+] version_tag: PASS  (version=2.10.0)`
- `python3 scripts/pre_release_check.py --gate status-doc -v` → exit 0
- `python3 scripts/check_version_alignment.py` → `All versions aligned!` — the tracked set (80 spots, listed by the command) covers every spot that states the current version or release date: the package manifests, the four npm `package-lock.json` roots including the `../../sdk/typescript` link entry, `uv.lock`, the `README.md` metrics block, `docs/status/metrics/catalog.toml`, the `CHANGELOG.md` Unreleased line and every doc spot down to the `aragora==2.10.0` install commands, with version-plus-date spots held to `RELEASE_DATE`. Release history (`## [2.10.0]`, `### v2.10.0 Behavioral Changes`, these notes) is content and deliberately untracked
- Post-tag, in a clean venv: `python -m venv /tmp/a && /tmp/a/bin/pip install aragora==2.10.0 && /tmp/a/bin/python -c "import aragora; print(aragora.__version__)"` → `2.10.0`
- Post-tag: `gh run list -R synaptent/aragora --workflow publish-aragora.yml --event workflow_dispatch --limit 5` shows a `success` run on `v2.10.0`

## Contributors

- Founder: @an0mium
- Multi-agent execution: Claude Code, Codex CLI, Factory droids (Receipt-First mission workers)
- Adversarial review: heterogeneous merge quorum (claude + openai western-frontier pair, grok/gemini/openrouter advisory families)

## Looking ahead — v2.11.0

- ODR v0.2 profile with the IETF ACTA/ASQAV mapping table, test vectors and `aragora-verify` 0.2.0
- File-based ODR signing-key custody and the served `.well-known` key
- Advisory dissent summaries posted on every quorum run
- Weekly Atlas regeneration as dated release assets
