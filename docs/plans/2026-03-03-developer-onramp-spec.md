# Developer Onramp — Dogfood-Generated Spec

> Generated by 4-model debate (Claude, OpenAI, Gemini, Grok) via `aragora ask` pipeline.
> Quality: 10.0/10, Practicality: 3.98/10 (path grounding weak — models referenced generic paths).

## Ranked High-Level Tasks

| Rank | Task | Priority | Confidence | Effort |
|------|------|----------|------------|--------|
| 1 | **Quickstart Guide** — install, API key setup, 3-line example | P0 | High | 3-6d |
| 2 | **Auto-generated /docs Page** — OpenAPI-driven reference docs | P0 | Medium | 3-6d |
| 3 | **Interactive API Playground** — Live endpoint tester | P1 | Medium | 5-9d |
| 4 | **SDK Parity** — Python + TypeScript core capabilities | P1 | Medium | 5-14d |
| 5 | **Integration Testing** — Validate onramp quality | P1 | High | 3-6d |

## Suggested Subtasks

### Quickstart Guide
1. Create `docs/quickstart.md` with:
   - `pip install aragora`/`npm install aragora`
   - API key setup via `ARAGORA_API_KEY`
   - 3-line debate example
2. Add verified examples:
   - `examples/quickstart.py`
   - `examples/quickstart.ts`
3. Implement CI validation:
   - Docker test environments
   - End-to-end script verification

### /docs Page
1. Validate `api/openapi.yaml` coverage
2. Integrate Redoc/Swagger UI at `/docs`
3. Add CI spec validation
4. Implement tabbed code samples

### API Playground
1. Build React component:
   - `frontend/components/Playground/`
   - Endpoint selector + parameter inputs
2. Add rate-limited proxy:
   - `api/middleware/sandbox.py`
3. Implement response visualizer

## Owner module / file paths

| Component | Path | Owner |
|-----------|------|-------|
| Quickstart | `docs/quickstart.md` | Docs |
| Examples | `examples/quickstart.{py,ts}` | SDK |
| OpenAPI | `api/openapi.yaml` | API |
| Docs UI | `frontend/pages/docs.tsx` | FE |
| Playground | `frontend/pages/api-playground.tsx` | FE |
| Python SDK | `sdks/python/aragora/` | SDK |
| TS SDK | `sdks/typescript/src/` | SDK |

## Test Plan

### Unit Tests
- SDK method parity (100% core)
- OpenAPI spec validation
- Quickstart script validation:
  - ≤3 lines user code
  - Successful debate response

### Integration Tests
- Time-to-first-debate:
  - Python: ≤300s
  - TypeScript: ≤300s
- Playground response: <10s
- Docs load: <3s (p95)

### E2E Tests
- Full onramp flow ≤5min
- Error message validation

## Rollback Plan

| Trigger | Action |
|---------|--------|
| Playground 5xx >5% | Revert `frontend/pages/api-playground.tsx` |
| SDK parity <100% | Rollback SDK releases |
| Docs load >3s | Switch to static docs |
| Key setup fails >10% | Restore prior auth docs |

## Gate Criteria

1. **Time-to-Debate**: ≤300s (Python/TS)
2. **Key Setup**: ≤60s manual test avg
3. **SDK Parity**: 100% core methods
4. **Playground**: <10s p99, <1% errors
5. **Docs Load**: <3s p95, 100% routes
6. **Success Rate**: ≥90% in walkthroughs

## JSON Payload
```json
{
  "tasks": [
    {"name": "Quickstart Guide", "priority": "P0", "confidence": "High", "days": [3,6]},
    {"name": "API Docs", "priority": "P0", "confidence": "Medium", "days": [3,6]},
    {"name": "API Playground", "priority": "P1", "confidence": "Medium", "days": [5,9]},
    {"name": "SDK Parity", "priority": "P1", "confidence": "Medium", "days": [5,14]}
  ],
  "metrics": {
    "timeToDebate": 300,
    "keySetupTime": 60,
    "sdkParity": 1.0,
    "playgroundLatency": 10,
    "docsLoadTime": 3
  }
}
```
